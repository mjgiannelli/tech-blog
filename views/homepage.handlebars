<ol>
    {{#each posts }}
    <li>
        <article>
            <div>
                <h2>{{title}}</h2>
                <span>Posted by {{user.username}} on {{created_at}}</span>
            </div>
            <div>
                <p>{{post_content}}</p>
            </div>
        </article>
    </li>
    {{/each}}
</ol>