<ol>
    <li>
        <article>
            <div>
                <h2>{{title}}</h2>
                <span>Posted by {{user.username}} on {{created_at}}</span>
            </div>
            <div>
                <p>{{post_content}}</p>
            </div>
        </article>
        <form id='comment-form'>
            <label for='comment'>Comment</label>
            <textarea name='comment' form='comment-form'>Enter comment here...</textarea>
        </form>
    </li>
</ol>